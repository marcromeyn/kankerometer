filepath = 'Sounds/Training/kanker';
dirData = dir(filepath);
dirIndex = [dirData.isdir];
fileList = {dirData(~dirIndex).name};
fvs = zeros(size(fileList));
i = 0;
for wav = fileList
    fv = getFeatureVectors(strcat(filepath, '/', cell2mat(wav)));
    fvs
end